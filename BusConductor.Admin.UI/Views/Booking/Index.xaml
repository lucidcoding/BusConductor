<Page x:Class="BusConductor.Admin.UI.Views.Booking.Index"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:BusConductor.Admin.UI.ViewModels.Bus" mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Index">
    <Page.DataContext>
        <!-- Declaratively create an instance of our SongViewModel -->
        <local:IndexViewModel />
    </Page.DataContext>

    <DataGrid x:Name="MainGrid" ItemsSource="{Binding Busses}" />
    
    <!--<Grid >
        <Label Grid.Column="0" Content="{Binding Busses[0].Name}"/>
    </Grid>-->
</Page>
